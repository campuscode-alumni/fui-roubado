<h1>Meus Relatórios</h1>
<p class="text-info"><%= link_to 'Reportar roubo', new_report_path %><p>

<% @reports.each do |report| %>
  <div class="card border-secondary mb-3" style="max-width: 20rem;">
    <div class="card-header"><%= link_to report.address, report %></div>
    <div class="card-body">
        <h4 class="card-title"><%= report.ocurrance_date%></h4>
        <% if report.police_report.attached? %>
            <%= image_tag report.police_report %>
        <% end %>
    </div>
  </div>
<% end %>