<% if user_signed_in? %>
  <p><%= "Bem vindo, #{current_user.name}." %></p>
<%elsif admin_signed_in? %>
  <%= "Bem vindo, #{current_admin.email}." %>  
<% end %>

<h2>Pesquisar item roubado</h2>
<%= form_tag search_path, method: :get do %>
  <%= label_tag :item_type_id, 'Tipo' %>
  <%= select_tag :item_type_id, options_from_collection_for_select(ItemType.all, :id, :name) %>
  <%= label_tag :q, 'Número de registro' %>
  <%= text_field_tag :q %>
  <%= submit_tag 'Buscar' %>
<% end %>