<h1>Fui roubado!</h1>
<% if user_signed_in? %>
  <p><%= "Bem vindo, #{current_user.email}." %></p>
  <p><%= link_to 'Cadastrar item', new_item_path %><p>
<%elsif admin_signed_in? %>
  <%= "Bem vindo, #{current_admin.email}." %>  
<% end %>

<h2>Busca</h2>
<%#= simple_form_for @item do |f| %>
  <%#= f.association :item_type%>
<%# end %>